import { ClassNames } from '@emotion/react'
import React from 'react'
import { NavLink } from 'react-router-dom'


export default function MainHeader() {
    return (
        <div>
            {/* <header className=''>
                <nav>
                    <ul>
                        <li>
                            <NavLink className={(navData) => {navData.isActive ? }} to='/'>All Meetups </NavLink>
                        </li>
                        <li>
                            <NavLink className={(navData) => {navData.isActive ? classes.active:''}} to='/newmeetup>'>NewMeetup</NavLink>
                        </li>
                        <li>
                            <NavLink className={(navData) => {navData.isActive ? classes.active:''}} to='/favourites>'>Favourites</NavLink>
                        </li>
                    </ul>
                </nav>
            </header> */}
        </div>
    )
}
